// version alternative
const WelcomeLambda = ({firstname, lastname }) => (
    <p>welkom page {firstname}{lastname} !</p>
);
  // -> la fonction lambda fait un return automatique
export default WelcomeLambda;